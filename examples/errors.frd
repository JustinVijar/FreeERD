// This file contains intentional errors for testing validation

title "Test Schema with Errors"

// Duplicate table name
table Users {
  id: int [pk],
  name: str
}

// Duplicate table name (ERROR)
table Users {
  user_id: int [pk],
  email: str
}

table Posts {
  id: int [pk],
  post_title: str,
  post_title: str,  // Duplicate column name (ERROR)
  author_id: int [fk]
}

// Invalid relationship - table doesn't exist (ERROR)
InvalidTable.id > Posts.author_id

// Invalid relationship - column doesn't exist (ERROR)
Users.nonexistent > Posts.author_id
